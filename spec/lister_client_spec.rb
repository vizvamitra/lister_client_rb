require 'spec_helper'

describe ListerClient do
  it 'has a version number' do
    expect(ListerClientRb::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
